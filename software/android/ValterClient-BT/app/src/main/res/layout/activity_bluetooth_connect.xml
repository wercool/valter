<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_bluetooth_connect"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="pro.valter.valterclient.BluetoothConnect">

    <TextView
        android:visibility="gone"
        android:text="No Bluetooth controller"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:id="@+id/noBTControllerTextView"
        android:layout_alignParentTop="true"
        tools:textColor="@android:color/holo_red_dark"
        android:textSize="24sp" />

    <ListView
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/pairedBTDevicesListView"
        android:layout_height="400dp"
        android:layout_above="@+id/rescanBT"/>

    <Button
        android:text="Re-scan BT in range"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/rescanBT"
        android:onClick="rescanBTClicked"/>

</RelativeLayout>
